<?php

return [
    /*
     * Настройки сущности бара (name, рабочие часы, open_cutoff_minutes)
     * живут в таблице `bars` (источник правды), читаются через Bar::default().
     * Здесь остаётся только конфигурация, не относящаяся к сущности бара.
     */
    'search' => [
        'per_page' => (int) env('BAR_SEARCH_PER_PAGE', 15),
    ],
];
